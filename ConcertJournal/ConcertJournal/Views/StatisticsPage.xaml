<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ConcertJournal.Views.StatisticsPage"
             xmlns:vm="clr-namespace:ConcertJournal.ViewModels"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             xmlns:controls="clr-namespace:ConcertJournal.Views.Controls"
             BackgroundColor="{DynamicResource Background}"
             x:DataType="vm:StatisticsViewModel">

    <Grid RowDefinitions="Auto,*,Auto">

        <VerticalStackLayout Grid.Row="0" Padding="20,15,20,5">
            <Label Text="Insights" FontSize="28" FontAttributes="Bold" TextColor="{DynamicResource Primary}"/>
            <Label Text="Your concert journey in numbers" FontSize="12" Opacity="0.7"/>
        </VerticalStackLayout>

        <material:TabView Grid.Row="1" x:Name="StatisticsTabView" TabPlacement="Top">

            <material:TabView.TabHeaderItemTemplate>
                <DataTemplate x:DataType="material:TabItem">
                    <Label Text="{Binding Title}" 
                           HorizontalTextAlignment="Center" 
                           VerticalTextAlignment="Center"
                           FontSize="14" 
                           Padding="10,15">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{Binding IsSelected}" Value="True">
                                <Setter Property="TextColor" Value="{DynamicResource Primary}" />
                                <Setter Property="FontAttributes" Value="Bold" />
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>
                </DataTemplate>
            </material:TabView.TabHeaderItemTemplate>

            <material:TabItem Title="Overview">
                <material:TabItem.ContentTemplate>
                    <DataTemplate x:DataType="vm:StatisticsViewModel">
                        <ScrollView Padding="15">
                            <VerticalStackLayout Spacing="15">
                                <Border BackgroundColor="{DynamicResource Primary}" Padding="20" StrokeShape="RoundRectangle 20">
                                    <VerticalStackLayout Spacing="5">
                                        <Label Text="LATEST MEMORY" FontSize="10" FontAttributes="Bold" TextColor="White" Opacity="0.8"/>
                                        <Label Text="{Binding LatestConcertText}" FontSize="20" FontAttributes="Bold" TextColor="White"/>
                                    </VerticalStackLayout>
                                </Border>

                                <Grid ColumnDefinitions="*,*" RowDefinitions="Auto" ColumnSpacing="15">
                                    <Border Grid.Column="0" BackgroundColor="{DynamicResource CardBackground}" Padding="15" StrokeShape="RoundRectangle 15">
                                        <VerticalStackLayout HorizontalOptions="Center" Spacing="5">
                                            <Label Text="Total" FontSize="12" Opacity="0.7" HorizontalOptions="Center"/>
                                            <Label Text="{Binding TotalConcertsText}" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center"/>
                                        </VerticalStackLayout>
                                    </Border>
                                    <Border Grid.Column="1" BackgroundColor="{DynamicResource CardBackground}" Padding="15" StrokeShape="RoundRectangle 15">
                                        <VerticalStackLayout HorizontalOptions="Center" Spacing="5">
                                            <Label Text="Avg Rating" FontSize="12" Opacity="0.7" HorizontalOptions="Center"/>
                                            <Label Text="{Binding AverageRating, StringFormat='{0:F1}'}" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{DynamicResource Primary}"/>
                                        </VerticalStackLayout>
                                    </Border>
                                </Grid>

                                <Border BackgroundColor="{DynamicResource CardBackground}" Padding="20" StrokeShape="RoundRectangle 15">
                                    <VerticalStackLayout Spacing="10">
                                        <Label Text="Overall Satisfaction" HorizontalOptions="Center" FontAttributes="Bold"/>
                                        <toolkit:RatingView Rating="{Binding AverageRating}" MaximumRating="5" IsReadOnly="True" 
                                                            FillColor="{DynamicResource Primary}" Shape="Star" ShapeDiameter="30" HorizontalOptions="Center"/>
                                    </VerticalStackLayout>
                                </Border>
                            </VerticalStackLayout>
                        </ScrollView>
                    </DataTemplate>
                </material:TabItem.ContentTemplate>
            </material:TabItem>

            <material:TabItem Title="Performers">
                <material:TabItem.ContentTemplate>
                    <DataTemplate x:DataType="vm:StatisticsViewModel">
                        <Grid RowDefinitions="Auto, Auto, *" Padding="15" RowSpacing="15">
                            <SearchBar Placeholder="Find an artist..." 
                           Text="{Binding SearchQuery}" 
                           HeightRequest="48"
                           TextColor="{DynamicResource TextColor}"/>

                            <HorizontalStackLayout Grid.Row="1" Spacing="10" HorizontalOptions="Center">
                                <Button Text="Top Hits" Command="{Binding SortMostCommand}" HeightRequest="40" CornerRadius="20"/>
                                <Button Text="Least" Command="{Binding SortLeastCommand}" HeightRequest="40" CornerRadius="20" 
                            BackgroundColor="Transparent" BorderWidth="1" BorderColor="{DynamicResource Primary}" 
                            TextColor="{DynamicResource TextColor}"/>
                            </HorizontalStackLayout>

                            <CollectionView Grid.Row="2" ItemsSource="{Binding DisplayedPerformers}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="vm:PerformerViewModel">
                                        <Border Margin="0,5" Padding="5" BackgroundColor="{DynamicResource CardBackground}" StrokeShape="RoundRectangle 12">
                                            <Grid ColumnDefinitions="Auto, *, Auto">
                                                <Border Grid.Column="0" WidthRequest="40" HeightRequest="40" StrokeShape="RoundRectangle 20" 
                                            BackgroundColor="{DynamicResource Primary}" Opacity="0.1" Margin="5"/>
                                                <Image Grid.Column="0" Source="{DynamicResource BandIcon}" WidthRequest="18" HorizontalOptions="Center"/>

                                                <Button Grid.Column="1" 
                                            Text="{Binding Name}" 
                                            Command="{Binding SelectPerformerCommand}"
                                            BackgroundColor="Transparent"
                                            TextColor="{DynamicResource TextColor}"
                                            FontAttributes="Bold"
                                            FontSize="16"
                                            HorizontalOptions="Start"
                                            Padding="10,0"
                                            BorderWidth="0"/>

                                                <Border Grid.Column="2" VerticalOptions="Center" Margin="0,0,10,0"
                                            Padding="12,4" StrokeShape="RoundRectangle 15" BackgroundColor="{DynamicResource Background}">
                                                    <Label Text="{Binding CountText}" FontSize="12" FontAttributes="Bold" TextColor="{DynamicResource Primary}"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </Grid>
                    </DataTemplate>
                </material:TabItem.ContentTemplate>
            </material:TabItem>

            <material:TabItem Title="Years">
                <material:TabItem.ContentTemplate>
                    <DataTemplate x:DataType="vm:StatisticsViewModel">
                        <ScrollView Padding="15">
                            <VerticalStackLayout Spacing="20">

                                <Border BackgroundColor="{DynamicResource Primary}" Padding="20" StrokeShape="RoundRectangle 20">
                                    <Grid ColumnDefinitions="*, Auto">
                                        <VerticalStackLayout Spacing="2">
                                            <Label Text="PEAK YEAR" FontSize="10" FontAttributes="Bold" TextColor="White" Opacity="0.8"/>
                                            <Label Text="{Binding MostConcertsYearText}" FontSize="24" FontAttributes="Bold" TextColor="White"/>
                                        </VerticalStackLayout>
                                        <Image Grid.Column="1" Source="{DynamicResource CalendarIcon}" WidthRequest="40" Opacity="0.5" VerticalOptions="Center"/>
                                    </Grid>
                                </Border>

                                <VerticalStackLayout Spacing="10">
                                    <Label Text="Annual Activity" FontAttributes="Bold" FontSize="18" Margin="5,0"/>

                                    <Border BackgroundColor="{DynamicResource CardBackground}" Padding="20" StrokeShape="RoundRectangle 16">
                                        <VerticalStackLayout Spacing="15">
                                            <Label Text="Concerts per Year" FontSize="12" FontAttributes="Bold" Opacity="0.5" TextTransform="Uppercase"/>
                                            <BoxView HeightRequest="1" Color="{DynamicResource Background}" Opacity="0.5"/>

                                            <Label FormattedText="{Binding ConcertsByYearFormatted}" 
                                       HorizontalOptions="Start" 
                                       FontSize="16"
                                       LineHeight="1.6"/>
                                        </VerticalStackLayout>
                                    </Border>
                                </VerticalStackLayout>

                            </VerticalStackLayout>
                        </ScrollView>
                    </DataTemplate>
                </material:TabItem.ContentTemplate>
            </material:TabItem>

            <material:TabItem Title="Geography">
                <material:TabItem.ContentTemplate>
                    <DataTemplate x:DataType="vm:CountryChartViewModel">
                        <Grid RowDefinitions="Auto, *" Padding="15">
                            <Label Text="World Tour Progress" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,10"/>
                            <lvc:CartesianChart
                                x:Name="CountriesChart"
                                BindingContext="{Binding ChartVM}"
                                Series="{Binding CountrySeries}"
                                XAxes="{Binding XAxes}"
                                YAxes="{Binding YAxes}" 
                                HeightRequest="{Binding ChartHeight}"
                                Background="{DynamicResource Background}"
                                TooltipPosition="Hidden"
                                LegendPosition="Bottom"/>
                        </Grid>
                    </DataTemplate>
                </material:TabItem.ContentTemplate>
            </material:TabItem>

        </material:TabView>

        <controls:BottomNavBar Grid.Row="2" ActiveRoute="StatisticsPage"/>
    </Grid>
</ContentPage>